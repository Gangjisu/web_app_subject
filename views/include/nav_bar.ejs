<link rel="stylesheet" href="/css/components/nav_bar.css">

<nav id="nav-bar" class="fixed top-0 left-0 w-full h-20 glass-panel z-50 flex items-center justify-between px-8 border-b border-white/20">
    <div class="flex items-center gap-4 cursor-pointer" onclick="typeof resetView === 'function' ? resetView() : location.href='/'">
        <!-- Mini Logo -->
        <svg width="60" height="36" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M22 2L11 13" stroke="#ffffff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M22 2L15 22L11 13L2 9L22 2Z" stroke="#ffffff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg> 
        <span class="font-bold text-xl tracking-wider lobster-regular" style="font-size: 30pt; margin-left: -25px;">Air Plan</span>
    </div>
    
    <div class="flex items-center gap-4">
        <button class="glass-btn px-4 py-2 rounded-full text-sm font-medium flex items-center gap-2">
            <i data-lucide="layout-dashboard" class="w-4 h-4"></i> 대시보드
        </button>
        <button class="glass-btn px-4 py-2 rounded-full text-sm font-medium bg-blue-500/30 border-blue-400/50 hover:bg-blue-500/50 flex items-center gap-2">
            <i data-lucide="plus" class="w-4 h-4"></i> 새 계획
        </button>
        <button onclick="typeof expandView === 'function' ? expandView() : null" class="glass-btn p-2 rounded-full hover:bg-white/30" title="메뉴 펼치기">
            <i data-lucide="chevron-down" class="w-5 h-5"></i>
        </button>
    </div>
</nav>